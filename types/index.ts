export enum ShippingType {
    IN_STORE = "in_store",
    STANDARD = "standard",
    EXPRESS = "express"
}

export enum Status {
    "PENDING" = "pending",
    "SHIPPED" = "shipped",
    "DELIVERED" = "delivered",
    "CANCELED" = "cancelled",
}